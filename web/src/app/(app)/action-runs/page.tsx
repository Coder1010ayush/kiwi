import { ActionRunsWorkspace } from "@/components/actions-chat/action-runs-workspace";

export const metadata = {
  title: "Action Runs",
};

export default function ActionRunsPage() {
  return <ActionRunsWorkspace />;
}
